title: GTFOBins

url: https://gtfobins.github.io
baseurl: /new # TODO temporary

exclude:
  - /Dockerfile
  - /Gemfile*
  - /LICENSE
  - /Makefile
  - /README.md
  - /linter/
  - /vendor/ # XXX this needs to be explicitly added

permalink: pretty

collections:
  gtfobins:
    output: true

defaults:
  - scope:
      path: _gtfobins
    values:
      layout: gtfobin
