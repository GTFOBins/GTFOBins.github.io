---
functions:
  inherit:
  - code: |-
      echo '...' >/path/to/temp-file
      needrestart -c /path/to/temp-file
    comment: |-
      This allows to run Perl code (`...`).
    contexts:
      sudo:
      unprivileged:
    from: perl
...
