---
functions:
  file-read:
  - code: |-
      awk '//' /path/to/input-file
    contexts:
      sudo:
      suid:
      unprivileged:
  file-write:
  - code: |-
      awk 'BEGIN { print "DATA" > /path/to/output-file }'
    contexts:
      sudo:
      suid:
      unprivileged:
  shell:
  - code: |-
      awk 'BEGIN {system("/bin/sh")}'
    contexts:
      sudo:
      suid:
        shell: true
      unprivileged:
...
