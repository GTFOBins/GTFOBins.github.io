---
functions:
  inherit:
  - code: |-
      rpmquery --eval '%{lua:...}'
    comment: |-
      This allows to run Lua code (`...`).
    from: lua
    version: Some older version is required.
  shell:
  - code: |-
      rpmquery --eval '%(/bin/sh 1>&2)'
    contexts:
      sudo:
      suid:
        shell: true
      unprivileged:
...
