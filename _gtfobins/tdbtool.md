---
description: Manipulates the contents of TDB files, commonly used in in Samba
functions:
  shell:
    - code: |
        tdbtool
        ! /bin/bash
  sudo:
    - code: |
        sudo tdbtool
        ! /bin/bash
  setuid:
    - code: |
        tdbtool
        ! /bin/bash
---
