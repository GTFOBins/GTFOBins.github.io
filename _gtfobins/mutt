---
functions:
  file-read:
  - binary: false
    code: |-
      mutt -F /path/to/input-file
    comment: |-
      The file is leaked as error messages.
    contexts:
      sudo:
      unprivileged:
...
