---
functions:
  shell:
    - description: This uses the mail command to run an executable shell.
      code: |
        mail --exec='!/bin/sh'
  sudo:
    - description: This uses the mail command to run an executable shell.
      code: |
        sudo mail --exec='!/bin/sh'
---
