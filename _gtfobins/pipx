---
comment: |-
  pipx can run Python code provided in a local script.
functions:
  inherit:
  - code: |-
      echo '...' >/path/to/file.py
      pipx run /path/to/file.py
    comment: |-
      This runs Python code (`...`) from the specified file.
    contexts:
      sudo:
      unprivileged:
    from: python

  shell:
  - code: |-
      pipx run /path/to/file.py
    contexts:
      sudo:
      unprivileged:
...
