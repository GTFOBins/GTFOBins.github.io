---
functions:
  file-read:
  - binary: false
    code: |-
      eqn /path/to/input-file
    comment: |-
      The content is actually parsed and corrupted by the command.
    contexts:
      sudo:
      suid:
      unprivileged:
...
