---
functions:
  inherit:
  - code: |-
      easy_install .
    comment: |-
      This executes a Python script named `setup.py` in the directory passed as argument (`.`).
    contexts:
      sudo:
      unprivileged:
    from: python
...
